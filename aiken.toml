name = "hello/world"        # {organisation}/{project}
version = "0.0.0"
license = "Apache-2.0"
description = "Aiken contracts for project 'hello/world'"

[repository]                # additional structured information on a project to show in generated documentation
user = "ariady-putra"
project = "world"
platform = "github"         # `github`, `gitlab` or `bitbucket`

[[dependencies]]            # `aiken packages`
name = "aiken-lang/stdlib"
version = "main"            # branch or git commit hash
source = "github"           # `github`, `gitlab` or `bitbucket`
